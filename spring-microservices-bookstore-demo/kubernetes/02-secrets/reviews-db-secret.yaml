apiVersion: v1
kind: Secret
metadata:
  name: reviews-db-credentials
  namespace: cloudshelf
type: Opaque
stringData:
  # IMPORTANT: Replace RDS_ENDPOINT_HERE with actual RDS endpoint after terraform apply
  # Get it with: aws rds describe-db-instances --db-instance-identifier cloudshelf-db --query 'DBInstances[0].Endpoint.Address' --output text --region us-east-1
  SPRING_DATASOURCE_URL: "jdbc:postgresql://RDS_ENDPOINT_HERE:5432/reviews_db"
  SPRING_DATASOURCE_USERNAME: "reviews_user"
  SPRING_DATASOURCE_PASSWORD: "ReviewsSecure123!"